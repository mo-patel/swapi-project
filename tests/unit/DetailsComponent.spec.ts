describe("DetailsComponent", () => {
  let wrapper;

  it("Columns exist", () => {
    //Search for Name, Height, Mass, Created, Edited, Planet Name
    fail("Test unfinished");
  });

  it("Check record exists", () => {
    //check Darth Vader exist with a height of 202
    fail("Test unfinished");
  });

  it("Clicking planet name shows popup", () => {
    //check function that triggers popup component
    fail("Test unfinished");
  });

  it("Data can be sorted", () => {
    //provide sample data and check if sorting function works
    fail("Test unfinished");
  });
});
