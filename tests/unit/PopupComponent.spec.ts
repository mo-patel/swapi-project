describe("PopupComponent.vue", () => {
  let wrapper;

  it("Show Relevant Fields", () => {
    //check for Name, Diameter, Climate and Population
    fail("Test unfinished");
  });

  it("Can hide Popup", () => {
    //check function for user to hide the popup
    fail("Test unfinished");
  });
});
