describe("Initial load checks", () => {
  let wrapper;
  it("Table renders", () => {
    //test if table exists
    fail("Test unfinished");
  });

  it("Record exists", () => {
    // test for Luke Skywalker in name
    fail("Test unfinished");
  });

  it("API returns data", () => {
    // Content exists in strict type
    fail("Test unfinished");
  });
});
